#!/usr/bin/env groovy

pipeline {
    stages {
        stage('Deploy') {
            steps {
                script {
                    echo "Deploy application"
                }
            }
        }
    }
}